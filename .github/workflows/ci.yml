name: CI

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend-validation:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      PYTHONPATH: app
      UV_CACHE_DIR: ${{ github.workspace }}/runtimes/cache/uv
      PYTHONPYCACHEPREFIX: ${{ github.workspace }}/runtimes/cache/pycache
      RUFF_CACHE_DIR: ${{ github.workspace }}/runtimes/cache/ruff
      MYPY_CACHE_DIR: ${{ github.workspace }}/runtimes/cache/mypy
      PIP_CACHE_DIR: ${{ github.workspace }}/runtimes/cache/pip
      COVERAGE_FILE: ${{ github.workspace }}/runtimes/cache/coverage/.coverage
      HF_HOME: ${{ github.workspace }}/runtimes/cache/huggingface
      HF_HUB_CACHE: ${{ github.workspace }}/runtimes/cache/huggingface/hub
      HF_ASSETS_CACHE: ${{ github.workspace }}/runtimes/cache/huggingface/assets
      HF_XET_CACHE: ${{ github.workspace }}/runtimes/cache/huggingface/xet
      TORCH_HOME: ${{ github.workspace }}/runtimes/cache/torch
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          version: "0.11.30"
          enable-cache: true
          cache-dependency-glob: app/server/uv.lock

      - name: Install locked test dependencies
        run: uv sync --locked --project app/server --extra test

      - name: Lint Backend
        run: uv run --project app/server ruff check --isolated app/server app/tests

      - name: Compile Backend
        run: uv run --project app/server python -m compileall app/server/app.py app/server/api app/server/common app/server/configurations app/server/contracts app/server/repositories app/server/services

      - name: Run Backend Tests
        run: uv run --project app/server python -m pytest -c pytest.ini app/tests/unit/server app/tests/e2e/server/test_execution_lifecycle.py app/tests/e2e/server/test_database_workflow_nodes.py --basetemp=runtimes/cache/pytest-tmp

  frontend-validation:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      npm_config_cache: ${{ github.workspace }}/runtimes/cache/npm
      PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/runtimes/cache/playwright/browsers
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22.13.0"
          cache: npm
          cache-dependency-path: app/client/package-lock.json

      - name: Install Frontend Dependencies
        working-directory: app/client
        run: npm ci --no-audit --no-fund

      - name: Lint Frontend
        working-directory: app/client
        run: npm run lint

      - name: Build Frontend
        working-directory: app/client
        run: npm run build

      - name: Run Frontend Unit Tests
        working-directory: app/client
        run: npm run test:unit
