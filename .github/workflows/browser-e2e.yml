name: Browser E2E

on:
  workflow_dispatch:
  schedule:
    - cron: "17 3 * * 1-5"

permissions:
  contents: read

concurrency:
  group: paragraph-browser-e2e-${{ github.workflow }}
  cancel-in-progress: true

jobs:
  browser:
    name: Browser E2E (mocked UI and live backend contract)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      FASTAPI_HOST: 127.0.0.1
      FASTAPI_PORT: 5002
      UV_CACHE_DIR: ${{ github.workspace }}/runtimes/cache/uv
      PYTHONPYCACHEPREFIX: ${{ github.workspace }}/runtimes/cache/pycache
      RUFF_CACHE_DIR: ${{ github.workspace }}/runtimes/cache/ruff
      MYPY_CACHE_DIR: ${{ github.workspace }}/runtimes/cache/mypy
      PIP_CACHE_DIR: ${{ github.workspace }}/runtimes/cache/pip
      HF_HOME: ${{ github.workspace }}/runtimes/cache/huggingface
      HF_HUB_CACHE: ${{ github.workspace }}/runtimes/cache/huggingface/hub
      HF_ASSETS_CACHE: ${{ github.workspace }}/runtimes/cache/huggingface/assets
      HF_XET_CACHE: ${{ github.workspace }}/runtimes/cache/huggingface/xet
      TORCH_HOME: ${{ github.workspace }}/runtimes/cache/torch
      npm_config_cache: ${{ github.workspace }}/runtimes/cache/npm
      PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/runtimes/cache/playwright/browsers
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Set up UV
        uses: astral-sh/setup-uv@v6
        with:
          version: "0.11.30"
          enable-cache: true
          cache-dependency-glob: app/server/uv.lock

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22.13.0"
          cache: npm
          cache-dependency-path: app/client/package-lock.json

      - name: Install locked backend dependencies
        run: uv sync --locked --project app/server --extra test

      - name: Install locked frontend dependencies
        working-directory: app/client
        run: npm ci --no-audit --no-fund

      - name: Start backend
        shell: bash
        run: |
          mkdir -p .ci-logs
          (cd app/server && uv run python -m uvicorn server.app:app --host 127.0.0.1 --port 5002 --log-level warning > "$RUNNER_TEMP/paragraph-backend.log" 2>&1 & echo $! > "$RUNNER_TEMP/paragraph-backend.pid")
          for i in {1..60}; do
            if curl --fail --silent --max-time 2 http://127.0.0.1:5002/docs >/dev/null; then exit 0; fi
            sleep 1
          done
          cat "$RUNNER_TEMP/paragraph-backend.log"
          exit 1

      - name: Install Playwright browser
        working-directory: app/client
        run: npx playwright install --with-deps chromium

      - name: Run mocked UI and live backend contract suites
        working-directory: app/client
        run: npm run test:e2e

      - name: Stop backend
        if: always()
        shell: bash
        run: |
          if [ -f "$RUNNER_TEMP/paragraph-backend.pid" ]; then
            kill "$(cat "$RUNNER_TEMP/paragraph-backend.pid")" || true
          fi

      - name: Upload browser diagnostics on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: paragraph-browser-diagnostics-${{ github.run_id }}
          retention-days: 3
          if-no-files-found: ignore
          path: |
            runtimes/cache/playwright/report/
            runtimes/cache/playwright/test-results/
            ${{ runner.temp }}/paragraph-backend.log
