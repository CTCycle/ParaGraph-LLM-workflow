# Custom Node Packs

Store custom node pack assets in this folder.

Suggested structure per pack:
- `<pack_name>/manifests/` for JSON manifests
- `<pack_name>/examples/` for sample workflows
- `<pack_name>/` for optional plugin scripts and helpers
