[package]
name = "paragraph-desktop"
version = "1.0.0"
description = "ParaGraph desktop shell"
authors = ["ParaGraph"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
