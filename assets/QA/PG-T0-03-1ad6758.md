# PG-T0-03 — Official Launcher Lifecycle and Warm Starts

Last updated: 2026-09-24

- Date: 2026-09-24
- Status: `PASS`
- Failure class: `ENV` — the access restrictions recorded by the 2026-09-23 partial run did not reproduce; the official launch, browser-open, conflict-cleanup, and option 2 paths completed on this host.
- Tested revision: `1ad67586cfc00e43db63d6048fb000547f8ad3b4`

## Scope

Validate the current Windows launcher option 1/2 lifecycle, configured health
endpoints, launch-owned process cleanup, approved port-conflict handling, and
three comparable warm starts. Re-run the deterministic launcher safety and
build-fingerprint harnesses. The app used a disposable resource root; the
configured `.env` was restored byte-for-byte afterward.

## Environment

- Windows 11 Pro, build 26200; PowerShell 7.6.6.
- Portable Python 3.14.2, uv 0.11.12, and Node.js 22.12.0.
- The official launcher opened its configured system browser. The in-app
  Browser was used for the workflow checks in the adjacent Tier 1 reports.
- Backend port 5002 and frontend port 8002; both were clear before and after
  validation. The temporary browser-only preview on 8003 was also stopped.
- The app resource root pointed to a disposable directory under
  `runtimes/cache/validation/20260924`; no user database was used.

## Executed scenarios

| Scenario | Result |
| --- | --- |
| Official launcher option 1 with current dependencies and build fingerprint. | `PASS` — backend `/docs` on port 5002 and frontend preview on port 8002 became healthy; the launcher opened the system browser. |
| Official option 2 process-tree cleanup. | `PASS` — four cleanup cycles returned successfully and the configured ports were confirmed clear. |
| Live controlled conflict on port 5002. | `PASS` — a listener created for this check (PID 1520, Python, bound to 127.0.0.1:5002) was identified by PID and command line. The launcher displayed the occupied-port warning; after confirmation, it stopped that exact listener, confirmed the ports free, and completed option 1. Official option 2 then cleaned up the launched app. |
| Three comparable warm starts. | `PASS` — measured from submitting launcher menu option 1 until the launcher exited, after a successful option 2 cleanup each time: 18.181 s, 16.153 s, and 14.960 s. Dependencies and frontend build were already current and therefore skipped consistently. |
| Launcher safety-race harness on the tested revision. | `PASS` — all 5 checks, including replacement-listener protection. |
| Frontend build-fingerprint harness on the tested revision. | `PASS` — all cases, including input invalidation, state classification, successful fingerprinting, and failed-build handling. |

## Validation commands

- `pwsh -NoProfile -ExecutionPolicy Bypass -File app/tests/test_launcher_safety_races.ps1` — 5/5 passed.
- `pwsh -NoProfile -ExecutionPolicy Bypass -File app/tests/test_launcher_build_fingerprint.ps1` — all harness cases passed.
- The earlier [environment-limited report](PG-T0-03-6e74193.md), [safety-race report](PG-T0-03-3de7520.md), and [build-fingerprint report](PG-T0-03-3010c00.md) remain historical provenance; this report reruns their relevant current-revision gates.

## Remaining limits

No launcher defect was found. This gate does not validate provider-backed
execution, custom-node import, or external integrations. The run used a
disposable data root and did not inspect or modify the user's normal app data.
