# PG-T0-03 Launcher Build-Fingerprint Matrix and Startup Attempt

- Date: 2026-09-23
- Status: **PARTIAL**
- Failure class: **ENV**
- Tested revision: 3010c00af36e9d9a2c2f2243f6d0042137a4a8d8 (fingerprint harness); live official launcher attempt used 2144ed15030e7c872fefb84216398c49e7a76c51, which has the same start_on_windows.ps1.

## Scope

Exercise the production launcher fingerprint and build-state functions in a
disposable fixture. Attempt one official option 1 warm start after confirming
dependencies and the existing build were ready and the configured ports were
clear. A completed warm-start sample requires a successful option 1 return and
verified option 2 cleanup.

## Environment

- Windows 10.0.26200.0; PowerShell 7.6.6.
- Portable Python 3.14.2, uv 0.11.12, Node.js 22.12.0.
- Configured ports were backend 5002 and frontend 8002; both were clear before
  the attempt and after cleanup.
- Frontend dependencies were ready and the launcher build fingerprint was
  current before option 1. No .env or runtime data was changed.

## Executed Scenarios

| Scenario | Result |
| --- | --- |
| Each discovered source/public file and each fixed frontend build input changes the fingerprint when edited, then restores the original fingerprint when restored. | **PASS** |
| Changing VITE_API_BASE_URL invalidates the fingerprint; changing runtime-only backend/frontend hosts and ports does not. | **PASS** |
| Missing output, missing fingerprint, invalid fingerprint, changed inputs, unavailable required input, and current build state are classified correctly. | **PASS** |
| Build-Frontend invokes npm run build, writes a current fingerprint after success, reports a nonzero build exit, and removes the old fingerprint after failure. | **PASS** |
| Official launcher option 1 with ready dependencies and current build. | **PARTIAL** — backend docs and frontend preview health checks succeeded. The launcher then failed to open http://127.0.0.1:8002; Windows returned Access denied from Start-Process. |
| Launcher automatic cleanup and official option 2 cleanup. | **BLOCKED** — taskkill.exe returned Access denied for launch-owned process trees and the backend listener. The exact process tree was verified by PID, parent PID, executable path, and launcher output, then stopped with Stop-Process. Both configured ports and all verified launch PIDs were clear afterward. |
| Three comparable warm-start timings. | **UNTESTED** — no accepted timing sample was recorded because option 1 returned failure and official cleanup was denied. No additional launch was attempted. |

## Validation

- pwsh -NoProfile -ExecutionPolicy Bypass -File app/tests/test_launcher_build_fingerprint.ps1 — all four harness cases passed.
- PowerShell parser checks passed for start_on_windows.ps1 and the new harness.
- git diff --check passed.
- The disposable fixture was removed. No screenshot, cache, credential, or runtime-data artifact is included.

## Remaining Gaps

PG-T0-03 remains PARTIAL. Repeat option 1 and option 2 on a Windows
environment that can open the configured URL and terminate processes created
by the validation session. Record three comparable warm-start samples there.
The scheduled Browser E2E failure on main is a separate gate and is not
covered by this report.
