# PG-T0-03 Official Launcher Revalidation

Last updated: 2026-09-23

- Date: 2026-09-23
- Status: **PARTIAL**
- Failure class: **ENV**
- Tested revision: 6e74193c1da4a329ac2249546f03b2a0691c9607

## Scope

Recheck the current official Windows option 1 and option 2 paths, frontend
preview reachability, process cleanup, and the focused launcher safety and
fingerprint harnesses. The test did not mutate the restored workflow or run a
model-backed workflow.

## Environment

- Windows 11 Pro, build 26200; PowerShell 7.6.6.
- Portable Python 3.14.2, uv 0.11.12, and Node.js 22.12.0.
- Codex In-app Browser; browser engine version is not exposed by the tool.
- Configured ports were backend 5002 and frontend 8002. Both were clear before
  and after the run.

## Executed Scenarios

| Scenario | Result |
| --- | --- |
| Official launcher option 1 with ready dependencies and a current build fingerprint. | **PARTIAL** — runtime setup and dependency/build work were skipped as current. The backend /docs health check passed after seven attempts and the frontend preview started. The launcher then failed to open http://127.0.0.1:8002 in the system browser with Access denied. |
| Frontend preview in the Codex In-app Browser. | **PASS** — the ParaGraph workflow builder rendered. The restored graph canvas, node cards, connections, navigation, and workflow controls were visible. The in-tool screenshot was inspected but no image file was retained in the repository. |
| Launcher automatic cleanup and official option 2. | **BLOCKED** — both paths received Access denied from taskkill.exe; option 2 reported exit code 1 while the launch-owned backend process remained alive. |
| Cleanup of launch-owned processes after the official cleanup denial. | **PASS** — process names, command lines, and parent relationships were checked for the exact Python, Node, and command-wrapper PIDs created by this launch. Those processes were stopped directly. Ports 5002 and 8002 were then confirmed clear. No unrelated process was targeted. |
| Launcher safety-race harness on the tested checkout. | **PASS** — all five focused checks passed. |
| Frontend build-fingerprint harness on the tested checkout. | **PASS** — all four harness cases passed, including input invalidation, state classification, successful build fingerprinting, and failed-build handling. |
| Three comparable warm-start timings. | **UNTESTED** — the official option 1 attempt ended with an environment error and no accepted timing sample was recorded. |

## Validation Commands

- pwsh -NoProfile -ExecutionPolicy Bypass -File app/tests/test_launcher_safety_races.ps1 — passed, 5/5 checks.
- pwsh -NoProfile -ExecutionPolicy Bypass -File app/tests/test_launcher_build_fingerprint.ps1 — passed, all four harness cases.
- git diff --check — passed for the tracked ledger and QA index changes.

## Remaining Gaps

PG-T0-03 remains **PARTIAL**. This environment can serve and render the preview
inside the Codex In-app Browser, but it denies the launcher's system-browser
open and taskkill process-tree cleanup. Direct cleanup succeeded only after
checking exact launch-owned process identities; that does not count as a
successful official option 2 run. The live approved port-conflict termination
also remains incomplete: the prior [safety-race report](PG-T0-03-3de7520.md)
records that the confirmation path reached taskkill but Windows denied the
termination. The current safety harness passes its deterministic checks. No
completed warm-start timing sample was recorded.

Re-run approved port-conflict termination, complete the option 1/2 lifecycle,
and collect three comparable warm-start samples on a Windows host where the
official browser open and taskkill are permitted. No product-code defect was
established in this run, so no launcher source change was made. Provider-backed
execution, custom-node import, and external integration gates remain separate
from this Tier 0 slice.
