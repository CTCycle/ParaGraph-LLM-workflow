# PG-T0-02 — Fresh Windows Bootstrap and Migrations

**Status:** `PASS` · **Tested revision:** `f43590f0084806ac0109066b7d96fa01a7506962` (`develop`) · **Date:** 2026-09-22

**Failure class:** `CFG` for the corrected test-run data-root setup; no application scenario failed.

**Environment:** Microsoft Windows NT 10.0.26200.0; PowerShell 7.6.6; portable Python 3.14.2; uv 0.12.17; Node.js 22.12.0; npm 10.9.0; Playwright Chromium 148.0.7778.96 (browser build 1223). The focused migration suite ran with Python 3.14.2 and pytest 9.0.3.

## Scope and isolation

The tracked revision was extracted with `git archive` into a temporary directory; no Git worktree was created. At baseline, `settings/.env` and `app/resources/database.db` were absent. The archive contained the repository's tracked node and workflow-template fixtures. All installer output, test databases, and temporary resources stayed inside the disposable snapshot. The existing checkout and its untracked `assets/cache/` were not modified.

This slice covers launcher option 3 and option 5, SQLite initialization and preservation, and the focused migration regression suite. Provider services and product browser workflows were out of scope.

## Executed scenarios

| Scenario | Result | Evidence |
| --- | --- | --- |
| Fresh option 3 with no `.env` or application database, Standard profile | `PASS` | Created `.env` from `.env.example`; installed portable Python, uv, Node, Python/frontend dependencies, and Playwright Chromium; initialized the database at `0003_canonical_provider_configuration`; production frontend build succeeded. `index.html` and the Chromium browser installation were present. |
| Repeat option 3 with the same profile | `PASS` | Runtime tools were reused; dependency sync, `npm ci`, database check, and frontend build completed again. The database was already synchronized at the same Alembic head. |
| Option 5 against a separate empty resources root, then repeat | `PASS` | First run created `alembic_version`, `chat_history_messages`, `configuration_profiles`, `execution_events`, `execution_runs`, `execution_steps`, `provider_configurations`, and `user_sessions` at `0003_canonical_provider_configuration`; SQLite `integrity_check` returned `ok`. Repeat reported the existing database synchronized; final table set, revision, and integrity were unchanged. |
| Option 5 against a partial unversioned schema | `PASS` | Launcher returned exit code 1 with `Database has application tables but no Alembic revision; refusing to adopt an unsupported unversioned schema.` The pre/post SHA-256 matched (`0E727789846FC6AB99CDF610187C70538BECD696343ECB5919D0D241BB8C58BD`); the `user_sessions` table definition and `preserve-me` row remained intact. |
| `app/tests/unit/server/repositories/test_database_migrations.py` against the archived source | `PASS` | 12 passed, 1 existing `chromadb` asyncio deprecation warning. The first invocation's session fixture inherited the incompatible scenario root and errored before assertions; after assigning the test run its own empty root, the full file passed. |

## Observations and limits

- The initial download attempt was denied by the sandbox network policy. Retrying with approved network access completed the setup; this was not counted as an application failure.
- Both `npm ci` runs completed but emitted `EBADENGINE`: dev-only `eslint-visitor-keys@5.0.1` declares Node.js `^20.19.0 || ^22.13.0 || >=24`, while the launcher installs Node.js 22.12.0. The Vite build still passed. See `ISSUE-004`.
- npm's install-time audit summary reported 16 vulnerabilities (1 low, 7 moderate, 7 high, 1 critical). Advisory details, production reachability, and remediation were not assessed in this slice. See `ISSUE-005`.
- Vite emitted the existing non-failing warning for a minified chunk larger than 500 kB. No frontend unit suite was run as part of this slice.

The `PASS` applies to the listed scenarios on the tested revision and environment only. Rerun after launcher, runtime, dependency-lock, or migration changes.
