{
  "id": "IF",
  "version": 1,
  "name": "If",
  "category": "control",
  "description": "Choose between two values based on a boolean input.",
  "inputs": [
    {"name": "condition", "data_type": "BOOLEAN", "required": true},
    {"name": "true_value", "data_type": "ANY", "required": true},
    {"name": "false_value", "data_type": "ANY", "required": true}
  ],
  "outputs": [
    {"name": "result", "data_type": "ANY", "required": true}
  ],
  "parameters": [],
  "ui": {"default_width": 280, "accent_color": "#a78bfa"},
  "runtime": {"executor_key": "if", "cacheable": true, "deterministic": true, "side_effecting": false}
}
