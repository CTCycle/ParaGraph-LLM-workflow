{
  "id": "CONTEXT_INJECTOR",
  "version": 1,
  "name": "Context Injector",
  "category": "processing",
  "description": "Format retrieval hits into prompt-ready context text with optional citations.",
  "inputs": [
    {"name": "results", "data_type": "RETRIEVAL_RESULTS", "required": true}
  ],
  "outputs": [
    {"name": "text", "data_type": "TEXT", "required": true}
  ],
  "parameters": [
    {
      "name": "max_context_items",
      "data_type": "JSON",
      "default": 5,
      "constraints": {"min": 1, "max": 50},
      "ui_control": "number",
      "description": "Maximum number of hits to inject into the prompt context."
    },
    {
      "name": "include_citations",
      "data_type": "BOOLEAN",
      "default": true,
      "ui_control": "toggle",
      "description": "Prefix each retrieved chunk with a numbered citation and source URI."
    },
    {
      "name": "separator",
      "data_type": "TEXT",
      "default": "\n\n",
      "ui_control": "text",
      "description": "Separator inserted between formatted retrieval chunks."
    }
  ],
  "ui": {"default_width": 340, "accent_color": "#0891b2"},
  "runtime": {"executor_key": "context_injector", "cacheable": true, "deterministic": true, "side_effecting": false}
}
