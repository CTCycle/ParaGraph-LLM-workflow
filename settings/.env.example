# ==============================================================================
# Backend Server
# ==============================================================================
FASTAPI_HOST=127.0.0.1
FASTAPI_PORT=5002
RELOAD=false
# Set to false to keep backend logs hidden.
BACKEND_LOGS_VISIBLE=true

# ==============================================================================
# Frontend Server
# ==============================================================================
UI_HOST=127.0.0.1
UI_PORT=8002
VITE_API_BASE_URL=/api

# Shared Resource Storage
# Leave blank to use app/resources. Set an absolute path or a path relative to the repository root to relocate all resource data, including the embedded SQLite database.
PARAGRAPH_RESOURCES_DIR=

# ==============================================================================
# Database Runtime
# ==============================================================================
# Internal application persistence always uses the embedded SQLite database.
DATABASE_INSERT_BATCH_SIZE=1000

# ==============================================================================
# Python / ML Runtime
# ==============================================================================
MPLBACKEND=Agg
KERAS_BACKEND=torch

# ==============================================================================
# LLM Runtime
# ==============================================================================
LLM_TIMEOUT_S=30
